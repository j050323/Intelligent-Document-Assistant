/**
 * REST API controllers.
 * Handles HTTP requests and responses for authentication and user management.
 * 
 * Controllers:
 * - AuthController: Authentication endpoints (register, login, password reset, etc.)
 * - UserController: User management endpoints (profile, avatar, password change, etc.)
 * - SystemLogController: System log query endpoints (admin only)
 * - GlobalExceptionHandler: Global exception handling for all controllers
 */
package com.docassistant.auth.controller;
